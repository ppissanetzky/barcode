{{!
This one is used to start a conversation between the person holding an item and
the next few people in line
    item - the item
    user - the user
    distances - undefined or an array of waiters with distance information
}}
{{user.name}} is done with the {{item.shortName}}

Please make arrangements with @{{user.name}} to exchange the {{item.shortName}}. {{#if user.location}}{{user.name}} is in {{user.location}}.{{/if}}
{{#if distances}}

[LIST]
{{#each distances}}
{{#if this.same}}
[*]@{{this.user.name}} is also in {{this.destinationName}} and is {{this.ordinal}} in line.
{{else}}
[*]@{{this.user.name}} is {{this.durationText}} ({{this.distanceText}}) away in {{this.destinationName}} and is {{this.ordinal}} in line.
{{/if}}
{{/each}}
[/LIST]
{{/if}}

[I]This message was automatically sent by BARcode[/I]
