{{!
This one is used to create a new post when a user says that they are
done using the item
    item - the item
    user - the user who is done
    distances - undefined or an array of waiters with distance information
}}
no title - this is a post

I'm done using the [URL={{equipmentUrl item.itemId}}]{{item.shortName}}[/URL]{{#if user.location}} in {{user.location}}{{/if}}.
{{#if distances}}

[LIST]
{{#each distances}}
{{#if this.same}}
[*]@{{this.user.name}} is also in {{this.destinationName}} and is {{this.ordinal}} in line.
{{else}}
[*]@{{this.user.name}} is {{this.durationText}} ({{this.distanceText}}) away in {{this.destinationName}} and is {{this.ordinal}} in line.
{{/if}}
{{/each}}
[/LIST]
{{/if}}

[B]Have the {{item.shortName}}[/B]

{{#each queue.haves}}
@{{this.user.name}} for {{this.age}}{{#if this.user.location}} - {{this.user.location}}{{/if}}{{#if this.isAvailable}} [B]AVAILABLE[/B]{{/if}}{{#if this.overdue}} [COLOR=red]OVERDUE[/COLOR]{{/if}}
{{/each}}

{{#if queue.waiters.length}}
[B]Waiting for the {{item.shortName}}[/B]

{{#each queue.waiters}}
@{{this.user.name}} {{this.ageWaiting}}{{#if this.user.location}} - {{this.user.location}}{{/if}}
{{/each}}
{{else}}
[B]No one waiting in line[/B]
{{/if}}

[I]This post was automatically created by BARcode[/I]
