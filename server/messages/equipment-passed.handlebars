{{!
This one is used to create a new post when a user gives the item to another
    item - the item
    fromUser - the user who gave the item
    toUser - the user that received it
}}
no title - this is a post

I gave the [URL={{equipmentUrl item.itemId}}]{{item.shortName}}[/URL] to @{{toUser.name}}.

[B]Have the {{item.shortName}}[/B]

{{#each queue.haves}}
@{{this.user.name}} for {{this.age}}{{#if this.user.location}} - {{this.user.location}}{{/if}}{{#if this.isAvailable}} [B]AVAILABLE[/B]{{/if}}{{#if this.overdue}} [COLOR=red]OVERDUE[/COLOR]{{/if}}
{{/each}}

{{#if queue.waiters.length}}
[B]Waiting for the {{item.shortName}}[/B]

{{#each queue.waiters}}
@{{this.user.name}} {{this.ageWaiting}}{{#if this.user.location}} - {{this.user.location}}{{/if}}
{{/each}}
{{else}}
[B]No one waiting in line[/B]
{{/if}}

[I]This post was automatically created by BARcode[/I]
