{{!
This one is used to create a new post when a user gets in line
    item - the item
    user - the user who got in line
}}
no title - this is a post

@{{user.name}} dropped out of line for the [URL={{equipmentUrl item.itemId}}]{{item.shortName}}[/URL].

[B]Have the {{item.shortName}}[/B]

{{#each queue.haves}}
@{{this.user.name}} for {{this.age}}{{#if this.user.location}} - {{this.user.location}}{{#if this.isAvailable}} [B]AVAILABLE[/B]{{/if}}{{/if}}{{#if this.overdue}} [COLOR=red]OVERDUE[/COLOR]{{/if}}
{{/each}}

{{#if queue.waiters.length}}
[B]Waiting for the {{item.shortName}}[/B]

{{#each queue.waiters}}
@{{this.user.name}} {{this.eta}}{{#if this.user.location}} - {{this.user.location}}{{/if}}
{{/each}}
{{else}}
[B]No one waiting in line[/B]
{{/if}}
